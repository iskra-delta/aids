{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Asteroids (F5)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/bin/asteroids",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": false,
      "MIMode": "gdb",
      "miDebuggerPath": "/usr/bin/gdb",
      "preLaunchTask": "build asteroids",
      "setupCommands": [
        {
          "description": "Enable pretty-printing for gdb",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ]
    }
  ]
}
